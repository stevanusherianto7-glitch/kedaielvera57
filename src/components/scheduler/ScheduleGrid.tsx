export default function ScheduleGrid({ employees, shifts, dates, onShiftClick }: any) {
  return <div className="p-4 border rounded-lg bg-slate-50">Jadwal Shift ({employees.length} Karyawan) (Placeholder)</div>;
}
